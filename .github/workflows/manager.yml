name: manager

on:
  pull_request:
    branches: [main]

permissions:
  pull-requests: write
  contents: read

jobs:
  rubocop:
    name: runner
    runs-on: ubuntu-latest

    steps:
      - uses: ruby/setup-ruby@e932e7af67fc4a8fc77bd86b744acd4e42fe3543
        with:
          ruby-version: 2.7.5
          bundler-cache: true

      - uses: actions/checkout@2541b1294d2704b0964813337f33b291d3f8596b
        with:
          fetch-depth: 0

      - name: install yq
        run: sudo wget -qO /usr/local/bin/yq https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64

      - name: manager review
        run: echo "managers=$(script/manager_review)" >> $GITHUB_ENV

      - uses: delivery-much/actions-assigner@v1
        with:
          reviewers: "${{ env.managers }}"
